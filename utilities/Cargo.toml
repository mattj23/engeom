[package]
name = "utilities"
version.workspace = true
edition.workspace = true

[dependencies]
engeom = {path = "../engeom", features = ["stl", "three_d", "ply"]}
clap = { version = "4.6.0" , features=["derive"]}