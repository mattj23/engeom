[package]
name = "utilities"
version.workspace = true
edition.workspace = true
# A repository-local CLI, not something to claim a crates.io name for.
publish = false

[dependencies]
engeom = {path = "../engeom", features = ["stl", "ply"]}
clap = { version = "4.6", features=["derive"]}